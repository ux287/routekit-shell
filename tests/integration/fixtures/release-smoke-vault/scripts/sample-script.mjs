// Sample script for release-smoke fixture. The embed pipeline should classify
// files under scripts/ as content_type='code' alongside src/. Second code file
// keeps the row-count sanity check non-trivial.

const main = () => {
  console.log("release-smoke-vault sample script");
};

main();
