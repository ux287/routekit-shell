# app.web.react.spa

Minimal React + Vite + TypeScript + Tailwind single-page-app starter for RouteKit.

Scaffold it:

```
routekit project init --stack app.web.react.spa --path <parent-dir>
```

Then `npm install && npm run dev` serves the app on localhost. Edit `src/App.tsx` to build your app.
